import { CommandInteraction, Guild, GuildMember, InteractionType, Message, TextBasedChannel } from "discord.js";
interface opt {
    content: string;
    ephemeral: boolean;
}
export default class RootComponent {
    entity: Message | CommandInteraction;
    type: InteractionType.MessageComponent | InteractionType.ApplicationCommand;
    channel: TextBasedChannel;
    guild: Guild;
    hostMember: GuildMember;
    reply: {
        3: (options: opt) => Promise<Message<boolean>> | Promise<import("discord.js").InteractionResponse<boolean>>;
        2: (options: opt) => Promise<Message<boolean>> | Promise<import("discord.js").InteractionResponse<boolean>>;
    };
    followUp: any;
    constructor(root: Message | CommandInteraction);
}
export {};
