import { EmbedBuilder } from "discord.js";
import { Question } from "open-trivia-db";
import TriviaGame from "./TriviaGame";
export default class EmbedGenerator {
    private readonly game;
    private readonly theme;
    constructor(game: TriviaGame);
    gameQueueStart(): EmbedBuilder;
    gameStart(): EmbedBuilder;
    leaderboardUpdate(question: Question): EmbedBuilder;
    finalLeaderboard(): EmbedBuilder;
    question(question: Question): EmbedBuilder;
}
